# 服务端口
server:
  port: 8302

# 服务名
spring:
  application:
    name: service-order
  profiles:
    active: dev
    # 返回json的全局时间格式
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.59.1:8848
        namespace: 5899d0a4-6587-4b89-b2ad-139b029ee54c
        group: dev
      config:
        server-addr: 192.168.59.1:8848
        namespace: 5899d0a4-6587-4b89-b2ad-139b029ee54c
        group: dev
  config:
    import:
      - optional:nacos:datebase.yml?group=dev&refreshEnabled=true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  # mybatis日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/*.xml